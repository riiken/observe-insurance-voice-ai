"""Narrow, purpose-specific tools the agent may invoke.

Each tool validates its input, enforces its own authorization boundary, calls a
service, and returns a structured result. Deliberately empty in Phase 1.
"""
